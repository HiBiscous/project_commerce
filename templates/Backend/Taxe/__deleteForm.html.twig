<form action="{{ path('admin.taxe.delete', {id: taxe.id}) }}" method="POST" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette taxe ?')">
	<input type="hidden" name="token" value="{{csrf_token('delete' ~ taxe.id)}}">
	<button type="submit" class="btn btn-danger">Supprimer</button>
</form>
